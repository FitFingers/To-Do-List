import React from "react";

function ErrorPage() {
    return (
        <div>
            <h1>Whoops, something's gone wrong!</h1>
            <p style={{fontSize: "0.7em"}}>Shouldn't be a problem; please try again!</p>
        </div>
    );
}

export default ErrorPage;
